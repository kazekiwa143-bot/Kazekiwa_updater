# Offset Updater — Render

Render Web Service settings:
- Runtime: Python 3
- Build Command: `pip install -r requirements.txt`
- Start Command: `python updater.py`
- Plan: Free

The site serves `index.html` and the mapping files under `maps/`.
